<h4>Encabezados de tabla final</h4>
<table class="table table-striped table-hover">
  <thead class="thead-inverse">
    <tr>
      <th class=""> Orden col. </th>
      <th class=""> ID </th>
      <th class=""> Nombre </th>
      <th class=""> Tipo campo </th>
      <th class=""> Tipo dato </th>
      <th class=""> Opciones </th>
    </tr>
  </thead>
  <tbody>
    <% bill_headers.each do |header| %>
      <tr>
        <td class=""> <%= header.order %> </td>
        <td class=""> <%= header.short_name %> </td>
        <td class=""> <%= header.name %> </td>
        <td class=""> <%= header.type_field %> </td>
        <td class=""> <%= header.type_value %> </td>
        <td class="center-text-v">
          <div class="btn-group" role="group" aria-label="Basic example">
            <div class="btn-group" role="group">
              <button id="btnGroupDrop1" type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fa fa-bars" aria-hidden="true"></i>
              </button>
              <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                <a class="dropdown-item" href="#">Dropdown link</a>
                <a class="dropdown-item" href="#">Dropdown link</a>
              </div>
            </div>
            <%= link_to fa_icon("eye"), "#", 'data-toggle' => 'modal', class: "btn btn-info" %>
            <%= link_to fa_icon("pencil"), edit_bill_header_path(header), class: "btn btn-warning" %>
            <%= link_to fa_icon("trash"), '#', class: "btn btn-danger" %>
          </div>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to fa_icon("plus"), new_bill_header_path, class: "btn btn-info" %>
